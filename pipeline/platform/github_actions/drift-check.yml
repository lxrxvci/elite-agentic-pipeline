name: Platform Workflow Drift Check

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  PROJECT_DIR: example_project

jobs:
  drift-check:
    uses: ./.github/workflows/_drift-check.yml
    with:
      project-dir: ${{ env.PROJECT_DIR }}
