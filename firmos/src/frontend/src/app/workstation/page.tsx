import { WorkstationView } from '@/page-views/workstation'

export const metadata = {
  title: 'Workstation — FirmOS',
  description: 'The daily driver: every work item, attributed to its accounting month.',
}

export default function WorkstationPage() {
  return <WorkstationView />
}
