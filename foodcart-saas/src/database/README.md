# Database Schema

The canonical schema is managed by Alembic migrations in `src/backend/alembic/versions/`.

To create or migrate the database:

```bash
cd src/backend
.venv/bin/alembic upgrade head
```

Do not edit this file or create manual SQL dumps. If you need a fresh schema,
run `alembic upgrade head` against an empty database.
